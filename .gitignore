# Generated at runtime from the wallpaper by zenith-shell's zenith-theme.sh,
# and seeded from scheme/default.lua by hyprland.lua when missing. Tracking it
# means every wallpaper change shows up as a repo modification.
scheme/current.lua
